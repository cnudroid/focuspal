name: CI - Test Suite

on:
  pull_request:
    branches: [ main, develop ]
  push:
    branches: [ main, develop ]
  workflow_dispatch:

env:
  DEVELOPER_DIR: /Applications/Xcode_15.2.app/Contents/Developer
  SCHEME: FocusPal
  DESTINATION: 'platform=iOS Simulator,name=iPhone 14,OS=17.2'

jobs:
  lint:
    name: SwiftLint
    runs-on: macos-14
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Run SwiftLint
        run: swiftlint lint --strict --reporter github-actions-logging

  unit-tests:
    name: Unit Tests
    runs-on: macos-14
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_15.2.app

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Run unit tests
        run: |
          xcodebuild test \
            -scheme ${{ env.SCHEME }} \
            -destination "${{ env.DESTINATION }}" \
            -only-testing:FocusPalTests \
            -enableCodeCoverage YES \
            -resultBundlePath TestResults/UnitTests.xcresult \
            | xcpretty --report junit

      - name: Generate coverage report
        run: |
          echo "Checking xcresult contents:"
          ls -laR TestResults/UnitTests.xcresult/ | head -50
          du -sh TestResults/UnitTests.xcresult
          if xcrun xccov view --report --json TestResults/UnitTests.xcresult > coverage.json 2>&1; then
            echo "Coverage report generated successfully:"
            head -100 coverage.json
          else
            echo "Warning: Could not generate coverage report"
            echo '{"lineCoverage": 0}' > coverage.json
          fi

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.json

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: unit-test-results
          path: TestResults/UnitTests.xcresult

  integration-tests:
    name: Integration Tests
    runs-on: macos-14
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_15.2.app

      - name: Run integration tests
        run: |
          xcodebuild test \
            -scheme ${{ env.SCHEME }} \
            -destination "${{ env.DESTINATION }}" \
            -only-testing:FocusPalTests/Integration \
            -enableCodeCoverage YES \
            -resultBundlePath TestResults/IntegrationTests.xcresult \
            | xcpretty

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-test-results
          path: TestResults/IntegrationTests.xcresult

  ui-tests:
    name: UI Tests
    runs-on: macos-14
    strategy:
      matrix:
        device:
          - 'iPhone SE (3rd generation)'
          - 'iPhone 14'
          - 'iPad (10th generation)'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_15.2.app

      - name: Boot simulator
        run: |
          SIMULATOR_ID=$(xcrun simctl list devices available | grep "${{ matrix.device }}" | grep -v "unavailable" | head -1 | awk -F'[()]' '{print $(NF-1)}')
          xcrun simctl boot $SIMULATOR_ID || true

      - name: Run UI tests
        run: |
          xcodebuild test \
            -scheme ${{ env.SCHEME }} \
            -destination "platform=iOS Simulator,name=${{ matrix.device }}" \
            -only-testing:FocusPalUITests \
            -enableCodeCoverage YES \
            -resultBundlePath "TestResults/UITests-${{ matrix.device }}.xcresult" \
            | xcpretty

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ui-test-results-${{ matrix.device }}
          path: "TestResults/UITests-${{ matrix.device }}.xcresult"

      - name: Upload screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ui-test-screenshots-${{ matrix.device }}
          path: ~/Library/Logs/DiagnosticReports

  code-coverage:
    name: Code Coverage
    needs: [unit-tests]
    runs-on: macos-14
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download coverage report
        uses: actions/download-artifact@v4
        with:
          name: coverage-report
          path: .

      - name: Check coverage threshold
        run: |
          COVERAGE=$(jq -r '.lineCoverage' coverage.json)
          THRESHOLD=80

          echo "Code coverage: ${COVERAGE}%"

          if [ "$COVERAGE" = "0" ] || [ "$COVERAGE" = "null" ]; then
            echo "⚠️ Coverage data not available - skipping threshold check"
            echo "This may indicate an issue with test coverage generation"
            exit 0
          elif (( $(echo "$COVERAGE < $THRESHOLD" | bc -l) )); then
            echo "❌ Code coverage ${COVERAGE}% is below threshold ${THRESHOLD}%"
            exit 1
          else
            echo "✅ Code coverage ${COVERAGE}% meets threshold ${THRESHOLD}%"
          fi

      - name: Upload coverage to Codecov
        continue-on-error: true
        uses: codecov/codecov-action@v3
        with:
          files: coverage.json
          fail_ci_if_error: false

  build:
    name: Build App
    runs-on: macos-14
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_15.2.app

      - name: Build for simulator
        run: |
          xcodebuild build \
            -scheme ${{ env.SCHEME }} \
            -destination "${{ env.DESTINATION }}" \
            -configuration Debug \
            | xcpretty

      - name: Build for device (Release)
        if: github.ref == 'refs/heads/main'
        run: |
          xcodebuild build \
            -scheme ${{ env.SCHEME }} \
            -destination 'generic/platform=iOS' \
            -configuration Release \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            | xcpretty

  performance-tests:
    name: Performance Tests
    runs-on: macos-14
    if: github.ref == 'refs/heads/main' || github.event_name == 'pull_request'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_15.2.app

      - name: Run performance tests
        run: |
          xcodebuild test \
            -scheme ${{ env.SCHEME }} \
            -destination "${{ env.DESTINATION }}" \
            -only-testing:FocusPalTests/Performance \
            -enableCodeCoverage NO \
            -resultBundlePath TestResults/PerformanceTests.xcresult \
            | xcpretty

      - name: Upload performance results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: performance-test-results
          path: TestResults/PerformanceTests.xcresult

  slack-notification:
    name: Slack Notification
    needs: [lint, unit-tests, integration-tests, ui-tests, code-coverage, build]
    runs-on: macos-14
    if: failure() && github.ref == 'refs/heads/main'
    steps:
      - name: Check if Slack is configured
        id: slack-check
        run: |
          if [ -n "${{ secrets.SLACK_WEBHOOK_URL }}" ]; then
            echo "configured=true" >> $GITHUB_OUTPUT
          else
            echo "configured=false" >> $GITHUB_OUTPUT
            echo "⚠️ Slack webhook not configured, skipping notification"
          fi

      - name: Send Slack notification
        if: steps.slack-check.outputs.configured == 'true'
        uses: slackapi/slack-github-action@v1
        with:
          payload: |
            {
              "text": "❌ FocusPal CI Failed on main branch",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*FocusPal Test Suite Failed*\n\nBranch: `${{ github.ref_name }}`\nCommit: `${{ github.sha }}`\nAuthor: ${{ github.actor }}"
                  }
                },
                {
                  "type": "actions",
                  "elements": [
                    {
                      "type": "button",
                      "text": {
                        "type": "plain_text",
                        "text": "View Workflow"
                      },
                      "url": "${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
